This project requires the Developer Certificate of Origin (DCO) process to be followed. The DCO is an attestation attached to every contribution made by every developer, signifying that the developer licenses their contribution under the open source license governing the file(s) being modified. In the commit message of the contribution, simply add a Signed-off-by statement, which signifies agreement to the text at https://developercertificate.org, reproduced in the DCO file in this repository.

The DCO requires a signoff message in the following format to appear on each commit:

Signed-off-by: Your Name <your.name@example.com>

If you set your user.name and user.email git configs, you can sign your commit automatically with git commit -s. 